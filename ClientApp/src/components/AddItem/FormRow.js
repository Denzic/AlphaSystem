import React from "react"
import { Col, Row, FormGroup } from "reactstrap"
import FormSet from "./FormSet"

const FormRow = ({ rowNumber }) => {
  const colNumber = [1, 2, 3]
  return <>{colNumber.map(())}</>
}

export default FormRow
